{% extends 'base.html.twig' %}

{% block title %}Logements{% endblock %}

{% block body %}
    <h1>Logements</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Type</th>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for logement in logements %}
            <tr>
                <td>{{ logement.type }}</td>
                <td>{{ logement.id }}</td>
                <td>
                    <a href="{{ path('app_logement_show', {'id': logement.id}) }}">Voir</a>
                    <a href="{{ path('app_logement_edit', {'id': logement.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Pas d'enregistrement</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_logement_new') }}">Créer un nouveau logement</a>
{% endblock %}
