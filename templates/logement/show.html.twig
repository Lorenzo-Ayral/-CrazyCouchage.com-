{% extends 'base.html.twig' %}

{% block title %}Logements{% endblock %}

{% block body %}
    <h1>Logements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Type</th>
                <td>{{ logement.type }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ logement.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_logement_index') }}">Revenir à la listet</a>

    <a href="{{ path('app_logement_edit', {'id': logement.id}) }}">Modifier</a>

    {{ include('logement/_delete_form.html.twig') }}
{% endblock %}
