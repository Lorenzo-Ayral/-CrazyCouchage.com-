<form method="post" action="{{ path('app_logement_delete', {'id': logement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ logement.id) }}">
    <button class="btn">Delete</button>
</form>
